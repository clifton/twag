# PII / Secret Exposure Scan Report

Generated by `scripts/pii_scan.py`. This report is regenerated by the
pytest guard in `tests/test_pii_scan.py`; do not hand-edit.

- Files scanned: **168**
- Findings (high): **0**
- Findings (medium): **0**
- Findings (low): **0**

No findings. The repository is clean per the configured rules.

## Rules

- `google_api_key` — _high_ — Google / Gemini API key literal (AIza...).
- `openai_api_key` — _high_ — OpenAI-style API key literal (sk-...).
- `anthropic_api_key` — _high_ — Anthropic API key literal (sk-ant-...).
- `github_token` — _high_ — GitHub personal access / app token literal.
- `slack_token` — _high_ — Slack token literal (xoxb-, xoxp-, ...).
- `aws_access_key` — _high_ — AWS access key id literal (AKIA...).
- `telegram_bot_token` — _high_ — Telegram bot token literal (digits:alnum).
- `twitter_auth_token_assignment` — _high_ — Twitter session cookie literal assigned to auth_token / ct0.
- `generic_secret_assignment` — _high_ — Generic secret-like assignment with quoted long literal.
- `risky_log_of_secret` — _medium_ — Logging / print statement that names a secret variable.
- `risky_fstring_secret` — _medium_ — f-string that interpolates a sensitive variable.
- `email_address` — _low_ — Email address literal (likely a PII / sample address).
- `us_phone_number` — _low_ — US-format phone number literal.

## Reviewed suppressions

Lines below were reviewed and deliberately suppressed via `# pii-scan: ignore`. They match scanner rules but are safe in context.

| Location | Line |
| --- | --- |
| `twag/cli/init_cmd.py:74` | `)  # pii-scan: ignore (instructional text, not a value)` |
| `twag/notifier.py:132` | `url = f"https://api.telegram.org/bot{bot_token}/sendMessage"  # pii-scan: ignore` |
